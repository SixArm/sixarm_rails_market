<%= begin_view_index(:Shops) %>

<table class="datatable">
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
      <th><%= t(:Name) %></th>
      <th><%= t(:Description) %></th>
    </tr>
  </thead>

  <tbody>
    <% @shops.each do |shop| %>
    <tr>
      <td><%= link_to_show shop %></td>
      <td><%= link_to_edit edit_shop_path(shop) %></td>
      <td><%= link_to_delete shop %></td>
      <td><%= shop.name %></td>
      <td><%= shop.description[0,30] %></td>
    </tr>
    <% end %>
  </tbody>

  <tfoot></tfoot>

</table>

<%= end_view_index(new_shop_path) %>

